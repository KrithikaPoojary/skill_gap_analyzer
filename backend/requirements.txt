"""Pinned Python dependency list for the backend.

Install with:
    pip install -r requirements.txt

For development/testing extras, also run:
    pip install -r requirements-dev.txt
"""

# Web framework
fastapi==0.115.12
uvicorn[standard]==0.34.3

# Configuration management (Pydantic v2 settings)
pydantic==2.11.7
pydantic-settings==2.7.1

# Async HTTP client (used in tests via HTTPX + anyio)
httpx==0.28.1

# Database and Migrations (SQLAlchemy 2.0 ORM + Alembic)
sqlalchemy>=2.0.0,<3.0.0
alembic>=1.13.0,<2.0.0

# Document Parsing – Resume Ingestion Pipeline (Day 10)
pypdf>=4.0.0          # PDF text extraction
python-docx>=1.1.0    # DOCX text extraction
python-multipart>=0.0.9  # Multipart form-data for FastAPI file uploads

# Authentication & Security
pyjwt>=2.8.0          # JWT encoding/decoding
bcrypt>=4.0.0         # Secure password hashing
passlib[bcrypt]>=1.7.4  # High-level password hashing facade
